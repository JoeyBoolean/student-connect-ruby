<div class="col-xs-12 col-sm-10 col-md-8 col-sm-offset-1 col-md-offset-2">
<h1>Listing Messages</h1>
    <% @messages.each do |message| %>
    <div class="well">
      <div class="media">
        <%= image_tag("no_image.jpg", :class => "media-object pull-left") %>
        <div class="media-body">
          <div class="media-heading">
            <% @user = @course.users.find(message.user_id) %>
            <h3><%=link_to @user.first_name, user_path(@user) %> said
              <span class="pull-right">
                <p class="date"><%= time_ago_in_words message.created_at %></p>
              </span>
            </h3>
              <p class="message-text"><%= message.message %></p>
              
            </div>
          </div>
        </div>
      </div>
    <% end %>

</div>
<br>

<%= link_to 'New Message', new_course_message_path(@course) %>
